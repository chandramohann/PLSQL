SELECT *
  FROM (SELECT alias.*, rownum Rno
          FROM (SELECT *
                  FROM (
                 
                  
                   )
                 ORDER BY 1) alias)
 WHERE Rno > 0 AND Rno <= 25